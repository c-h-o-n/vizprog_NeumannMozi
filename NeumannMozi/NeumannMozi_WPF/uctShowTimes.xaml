<UserControl x:Class="NeumannMozi_WPF.uctShowTimes"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NeumannMozi_WPF"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="White">

    <ScrollViewer>
        

    <Grid>
        <!-- Current Show Times -->
        <ItemsControl Name="ictrFilmCard" MinWidth="500">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="Black" BorderThickness="1" Margin="55">
                        <StackPanel Orientation="Vertical" MinWidth="500" MaxWidth="1000">
                            <Image Source="{Binding PosterLink}"/>
                            <TextBlock Text="{Binding Title}" TextWrapping="Wrap"  TextTrimming="WordEllipsis"/>
                            <TextBlock Text="{Binding Director}" TextWrapping="Wrap"  TextTrimming="WordEllipsis"/>
                            <TextBlock Text="{Binding Cast}" TextWrapping="Wrap"  TextTrimming="WordEllipsis"/>
                            <TextBlock Text="{Binding Description}" TextWrapping="Wrap"  TextTrimming="WordEllipsis"/>
                            <TextBlock Text="{Binding AgeRating}" TextWrapping="Wrap"  TextTrimming="WordEllipsis"/>
                            <TextBlock Text="{Binding Length}" TextWrapping="Wrap"  TextTrimming="WordEllipsis"/>
                            <TextBlock Text="{Binding Category}" TextWrapping="Wrap"  TextTrimming="WordEllipsis"/>
                            <Label Content="{Binding ScreeningDates}"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
    </ScrollViewer>
</UserControl>
